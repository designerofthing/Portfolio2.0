import React from "react";
import Header from "./Header";

const WebDev = ({ title }) => {
  return (
    <>
      <div id="portfolio">
        <Header title={title} />
      </div>
      <div id="webdev-page">
      </div>
    </>
  );
};

export default WebDev;
