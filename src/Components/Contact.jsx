import React from "react";
import Header from "./Header";

const Contact = ({title}) => {
  return (
    <div id="contact">
      <Header title={title}/>
      
    </div>
  );
};

export default Contact;
