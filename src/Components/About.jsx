import React from "react";
import Header from "./Header";

const About = ({title}) => {
  return (
    <div id="about">
      <Header title={title} />
      
     
    </div>
  );
};

export default About;
