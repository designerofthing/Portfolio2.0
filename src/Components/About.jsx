import React from "react";
import Header from "./Header";

const About = ({title}) => {
  return (
    <div id="about-header">
      <Header title={title} />
      
     
    </div>
  );
};

export default About;
